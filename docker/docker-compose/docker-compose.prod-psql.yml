services:
  app:
    image: danosoft/saldi:latest
    container_name: saldi_app
    ports:
      - "8080:80"
    environment:
      SALDI_DOCKER: ${SALDI_DOCKER:?set SALDI_DOCKER}
      DB_TYPE: ${DB_TYPE:?set DB_TYPE}
      DB_HOST: ${DB_HOST:?set DB_HOST}
      DB_NAME: ${DB_NAME:?set DB_NAME}
      DB_USER: ${DB_USER:?set DB_USER}
      DB_PASSWORD: ${DB_PASSWORD:?set DB_PASSWORD}
    volumes:
      # Only mount persistent user data
      - ./saldibilag:/var/www/html/saldibilag
      - ./backup:/backup
      - ./saldi_logolib:/var/www/html/logolib
    depends_on:
      - db
    restart: always

  db:
    image: postgres:15
    container_name: saldi_db
    environment:
      POSTGRES_DB: ${DB_NAME:?set DB_NAME}
      POSTGRES_USER: ${DB_USER:?set DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?set DB_PASSWORD}
    volumes:
      - pg_data:/var/lib/postgresql/data
    restart: always

volumes:
  pg_data:
