services:
  saldidb:
  db:
    image: postgres:15
    container_name: saldi_db
    environment:
      POSTGRES_DB: ${DB_NAME:?set DB_NAME}
      POSTGRES_USER: ${DB_USER:?set DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?set DB_PASSWORD}
    volumes:
      - saldi_data:/var/lib/postgresql/data
    restart: always

volumes:
  saldi_data:
